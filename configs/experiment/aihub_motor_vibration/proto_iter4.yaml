# @package _global_

# to execute this experiment run:
# python train.py experiment=example

defaults:
  - override /data: aihub_motor_vibration_proto.yaml
  - override /model: conv_lstm_classifier_proto_outputsize_control_mixup_triplet.yaml
  - override /callbacks: default.yaml
  - override /trainer: default.yaml

# all parameters below will be merged with parameters from default configurations set above
# this allows you to overwrite only specified parameters
seed: 1234

tags: ["aihub_motor_vibration", "ProtoNet_iter3_mixup"]

logger:
  wandb:
    project: "aihub_motor_vibration-ProtoNet2"
    tags: ['ProtoNet_iter3_mixup']
    name: "ProtoNet_iter3_mixup"